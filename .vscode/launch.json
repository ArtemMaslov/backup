{
    "version": "0.2.0",
    "configurations": 
    [
        {
            "name": "Backup launch",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceRoot}/bin/backup.out",
            "cwd": "${workspaceRoot}/bin",
            "preLaunchTask": "BuildBackup",
            "args": 
            [
                "./test", "./test_backup"
            ]
        }
    ]
}